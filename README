# Biance.py

---

Этот код выполняет вывод средств с биржи Binance на указанные адреса, используя приватные ключи из файла private_key.txt. Он также записывает логи операций в файл BINANCE_LOG.txt для отслеживания событий и ошибок. Убедитесь, что у вас установлены все необходимые библиотеки, и что в файле .env указаны правильные API-ключи Binance. Если при выводе средств на адрес возникнет ошибка, скрипт продолжит свою работу, залогирует ошибку и перейдет к следующему адресу без прерывания. Это позволяет обработать все адреса, даже если какая-то из операций вывода не удалась.
